---
description: TypeScript standards
globs: ["**/*.ts", "**/*.tsx"]
alwaysApply: false
---

# TypeScript Rules

- Do not use `any`.
- Do not use type assertions (`as Type`).
- Prefer type guards, discriminated unions, and explicit typing.
- Fix types instead of coercing values to fit.
